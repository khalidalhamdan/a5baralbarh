"use client"; import { useState } from "react"; import { useRouter } from "next/navigation";
export function MusicForm(){const router=useRouter(),[busy,setBusy]=useState(false),[message,setMessage]=useState("");async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setMessage("");const form=event.currentTarget,response=await fetch("/api/music",{method:"POST",body:new FormData(form)}),body=await response.json();setBusy(false);if(!response.ok)return setMessage(body.error);form.reset();setMessage("تم رفع الموسيقى واعتمادها للمكساج");router.refresh()}return <form className="adminForm" onSubmit={submit} id="music"><h3>الموسيقى المرخّصة</h3><p>سيتم اعتماد آخر ملف مرفوع كالمسار الافتراضي للمكساج.</p><div className="formGrid"><label>اسم المسار<input name="name" required/></label><label>نوع الترخيص<input name="licenseType" placeholder="ملكية / ترخيص تجاري" required/></label><label className="full">دليل الترخيص<input name="licenseEvidence" placeholder="رقم العقد أو رابط/وصف الإثبات" required/></label><label className="full">ملف الصوت<input name="file" type="file" accept="audio/*" required/></label><label className="check full"><input name="rightsConfirmed" type="checkbox" value="true" required/> أؤكد امتلاك حق الاستخدام التجاري في البودكاست</label></div><button disabled={busy}>{busy?"جارٍ الرفع…":"رفع واعتماد"}</button>{message&&<p className="formMessage">{message}</p>}</form>}
