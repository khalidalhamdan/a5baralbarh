"use client"; import { useState } from "react"; import { useRouter } from "next/navigation";
export function SourceForm(){const router=useRouter(),[busy,setBusy]=useState(false),[message,setMessage]=useState("");async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setMessage("");const form=event.currentTarget,values=Object.fromEntries(new FormData(form));const response=await fetch("/api/feeds",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({...values,priority:Number(values.priority)})});const body=await response.json();setBusy(false);if(!response.ok)return setMessage(body.error);form.reset();setMessage("تم اختبار المصدر وإضافته");router.refresh()}return <form className="adminForm" onSubmit={submit}><h3>إضافة مصدر موثوق</h3><div className="formGrid"><label>اسم المصدر<input name="name" required minLength={2}/></label><label>التصنيف<input name="category" defaultValue="عام" required/></label><label className="full">رابط RSS<input name="url" type="url" dir="ltr" required/></label><label>الأولوية<input name="priority" type="number" min="0" max="100" defaultValue="50" required/></label></div><button disabled={busy}>{busy?"جارٍ الاختبار…":"اختبار وإضافة"}</button>{message&&<p className="formMessage">{message}</p>}</form>}
