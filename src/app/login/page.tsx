"use client";
import { useState } from "react"; import { createBrowserClient } from "@supabase/ssr"; import { Radio } from "lucide-react";
export default function Login(){const[email,setEmail]=useState("");const[message,setMessage]=useState("");async function submit(e:React.FormEvent){e.preventDefault();const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!url||!key){setMessage("لم يتم إعداد Supabase بعد");return}const {error}=await createBrowserClient(url,key).auth.signInWithOtp({email,options:{emailRedirectTo:`${location.origin}/auth/callback`}});setMessage(error?error.message:"أرسلنا رابط الدخول الآمن إلى بريدك")};return <main style={{minHeight:"100vh",display:"grid",placeItems:"center",padding:20}}><form onSubmit={submit} className="panel" style={{width:"min(420px,100%)",padding:32,textAlign:"center"}}><span className="brandMark" style={{margin:"0 auto 18px"}}><Radio/></span><h1>دخول المالك</h1><p style={{color:"#758078"}}>أدخل البريد المعتمد لإرسال رابط دخول لمرة واحدة.</p><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="owner@example.com" style={{width:"100%",padding:13,border:"1px solid #dce4df",borderRadius:10,direction:"ltr",margin:"12px 0"}}/><button type="submit" style={{width:"100%",padding:12,border:0,borderRadius:10,background:"#176c4b",color:"white",font:"700 14px Tajawal"}}>إرسال رابط الدخول</button>{message&&<p>{message}</p>}</form></main>}
